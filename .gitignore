# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.sw?

# Rust / Tauri build outputs
src-tauri/target
src-tauri/gen/schemas

# Local configuration and environment files
.env
.env.local
.env.*.local

# Runtime cache of the app (only if launched from the project folder;
# normally stored in the OS app-data directory)
cache/

# makepkg output, next to the PKGBUILD it is built from
packaging/aur/src/
packaging/aur/pkg/
packaging/aur/*.tar.gz
packaging/aur/*.pkg.tar.*
# where CI unpacks the built package to inspect it
packaging/aur/inspect/
